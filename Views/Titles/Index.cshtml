@model IEnumerable<CsManager.Models.Title>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutM.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Title_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Title_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Title_ID })
        </td>
    </tr>
}

</table>
