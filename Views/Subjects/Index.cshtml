@model IEnumerable<CsManager.Models.Subject>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutM.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Subj_Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subj_DepID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subj_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subj_Credit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subj_CoursID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Subj_Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subj_DepID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subj_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subj_Credit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subj_CoursID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Subj_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Subj_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Subj_ID })
        </td>
    </tr>
}

</table>
